/*
 * Module code goes here. Use 'module.exports' to export things:
 * module.exports.thing = 'a thing';
 *
 * You can import it from another modules like this:
 * var mod = require('role.containerManager');
 * mod.thing == 'a thing'; // true
 */

module.exports = {
var roleFerry = {
    run:function(creep){
        //Find Flag
        //TODO Try to make this the first self running module
        //GOTO FLAG and set arrived
        //Find Overflowing container and move 100 to less full container in range 1;
        
    }
    
}

module.exports = roleFerry;
};